# Pre-Execution Validation (STEP 2.5) — SDD v3.8.0 MINOR Audit

> **Generated:** 2026-03-06 14:23
> **Patch:** v3.7.1 → v3.8.0 (MINOR)
> **Type:** MINOR — Added runtime pre-execution validation step to framework template.
> **Status:** ✅ Complete

---

## Goal
Add a lightweight, deterministic validation step (`STEP 2.5 — EXECUTION VALIDATION`) immediately prior to agent execution (`STEP 3 — EXECUTE`). This prevents hallucinated or unconstrained execution by enforcing context existence and strict matrix routing prior to any code planning.

## Scope of Changes

### 1. Master Agent Entrypoint Update
- **Modified `templates/project/prompts/02-agent-entrypoint.md`**:
  - Injected `STEP 2.5 — EXECUTION VALIDATION`.
  - Implemented 4 strict validation checks:
    1. **Inbox Task Check:** Validates `jobs/inbox.md` is not empty.
    2. **Task Classifiability Check:** Moved the routing matrix enforcement from STEP 3 into this pre-check step, retaining the `HARD STOP` if roles mismatch.
    3. **Required Context Check:** Enforces that required files (e.g. `docs/api-contract.md` for backend, `docs/spec.md` for architecture) exist and blocks if missing.
    4. **Framework Change Detection:** Explicitly flags the task if it modifies framework-owned paths (`core/`, `templates/`, `tools/`, `agents/`, `profiles/`, `task-types/`), activating the `AUTO-GIT` rules downstream in STEP 4.
  - Bumped internal entrypoint version to `3.8.0`.

### 2. Version Bump Rationale
- Because this change directly alters the mandatory runtime execution sequence (adding a new step that can trigger a `HARD STOP` if context files are missing), it represents a **behavior/workflow change**. Per Semantic Versioning and the framework rules, this dictates a **MINOR** version bump from `3.7.1` to `3.8.0`.

### 3. Documentation Alignment
- `CHANGELOG.md` updated with the `3.8.0` entry.
- `README.md` updated to reflect version `3.8.0` and to index this audit file.

## Definition of Done Verification
- `02-agent-entrypoint.md` contains STEP 2.5 (✅)
- STEP 2.5 performs the 4 specified validation checks (✅)
- Validation failures CLEARLY STOP execution (✅)
- Framework change detection integrates organically with existing STEP 4 logic (✅)
- README and CHANGELOG bumped to MINOR `3.8.0` (✅)
- Audit log written (✅)
- Committed to Git via Conventional Commits (✅)
